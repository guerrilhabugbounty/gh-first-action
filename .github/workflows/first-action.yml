name: First Workflow
on: workflow_dispatch 
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello World!"
      - name: Print Goodbye
        run: echo "Done, bye!"
    steps:
      - name: Show IP
        run: ifconfig
